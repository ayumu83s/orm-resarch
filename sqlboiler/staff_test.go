// This file is generated by SQLBoiler (https://github.com/vattle/sqlboiler)
// and is meant to be re-generated in place and/or deleted at any time.
// DO NOT EDIT

package sqlboiler

import (
	"bytes"
	"reflect"
	"testing"

	"github.com/vattle/sqlboiler/boil"
	"github.com/vattle/sqlboiler/randomize"
	"github.com/vattle/sqlboiler/strmangle"
)

func testStaffs(t *testing.T) {
	t.Parallel()

	query := Staffs(nil)

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}
func testStaffsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	staff := &Staff{}
	if err = randomize.Struct(seed, staff, staffDBTypes, true, staffColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = staff.Insert(tx); err != nil {
		t.Error(err)
	}

	if err = staff.Delete(tx); err != nil {
		t.Error(err)
	}

	count, err := Staffs(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testStaffsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	staff := &Staff{}
	if err = randomize.Struct(seed, staff, staffDBTypes, true, staffColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = staff.Insert(tx); err != nil {
		t.Error(err)
	}

	if err = Staffs(tx).DeleteAll(); err != nil {
		t.Error(err)
	}

	count, err := Staffs(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testStaffsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	staff := &Staff{}
	if err = randomize.Struct(seed, staff, staffDBTypes, true, staffColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = staff.Insert(tx); err != nil {
		t.Error(err)
	}

	slice := StaffSlice{staff}

	if err = slice.DeleteAll(tx); err != nil {
		t.Error(err)
	}

	count, err := Staffs(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}
func testStaffsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	staff := &Staff{}
	if err = randomize.Struct(seed, staff, staffDBTypes, true, staffColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = staff.Insert(tx); err != nil {
		t.Error(err)
	}

	e, err := StaffExists(tx, staff.StaffID)
	if err != nil {
		t.Errorf("Unable to check if Staff exists: %s", err)
	}
	if !e {
		t.Errorf("Expected StaffExistsG to return true, but got false.")
	}
}
func testStaffsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	staff := &Staff{}
	if err = randomize.Struct(seed, staff, staffDBTypes, true, staffColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = staff.Insert(tx); err != nil {
		t.Error(err)
	}

	staffFound, err := FindStaff(tx, staff.StaffID)
	if err != nil {
		t.Error(err)
	}

	if staffFound == nil {
		t.Error("want a record, got nil")
	}
}
func testStaffsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	staff := &Staff{}
	if err = randomize.Struct(seed, staff, staffDBTypes, true, staffColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = staff.Insert(tx); err != nil {
		t.Error(err)
	}

	if err = Staffs(tx).Bind(staff); err != nil {
		t.Error(err)
	}
}

func testStaffsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	staff := &Staff{}
	if err = randomize.Struct(seed, staff, staffDBTypes, true, staffColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = staff.Insert(tx); err != nil {
		t.Error(err)
	}

	if x, err := Staffs(tx).One(); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testStaffsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	staffOne := &Staff{}
	staffTwo := &Staff{}
	if err = randomize.Struct(seed, staffOne, staffDBTypes, false, staffColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}
	if err = randomize.Struct(seed, staffTwo, staffDBTypes, false, staffColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = staffOne.Insert(tx); err != nil {
		t.Error(err)
	}
	if err = staffTwo.Insert(tx); err != nil {
		t.Error(err)
	}

	slice, err := Staffs(tx).All()
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testStaffsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	staffOne := &Staff{}
	staffTwo := &Staff{}
	if err = randomize.Struct(seed, staffOne, staffDBTypes, false, staffColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}
	if err = randomize.Struct(seed, staffTwo, staffDBTypes, false, staffColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = staffOne.Insert(tx); err != nil {
		t.Error(err)
	}
	if err = staffTwo.Insert(tx); err != nil {
		t.Error(err)
	}

	count, err := Staffs(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}
func staffBeforeInsertHook(e boil.Executor, o *Staff) error {
	*o = Staff{}
	return nil
}

func staffAfterInsertHook(e boil.Executor, o *Staff) error {
	*o = Staff{}
	return nil
}

func staffAfterSelectHook(e boil.Executor, o *Staff) error {
	*o = Staff{}
	return nil
}

func staffBeforeUpdateHook(e boil.Executor, o *Staff) error {
	*o = Staff{}
	return nil
}

func staffAfterUpdateHook(e boil.Executor, o *Staff) error {
	*o = Staff{}
	return nil
}

func staffBeforeDeleteHook(e boil.Executor, o *Staff) error {
	*o = Staff{}
	return nil
}

func staffAfterDeleteHook(e boil.Executor, o *Staff) error {
	*o = Staff{}
	return nil
}

func staffBeforeUpsertHook(e boil.Executor, o *Staff) error {
	*o = Staff{}
	return nil
}

func staffAfterUpsertHook(e boil.Executor, o *Staff) error {
	*o = Staff{}
	return nil
}

func testStaffsHooks(t *testing.T) {
	t.Parallel()

	var err error

	empty := &Staff{}
	o := &Staff{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, staffDBTypes, false); err != nil {
		t.Errorf("Unable to randomize Staff object: %s", err)
	}

	AddStaffHook(boil.BeforeInsertHook, staffBeforeInsertHook)
	if err = o.doBeforeInsertHooks(nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	staffBeforeInsertHooks = []StaffHook{}

	AddStaffHook(boil.AfterInsertHook, staffAfterInsertHook)
	if err = o.doAfterInsertHooks(nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	staffAfterInsertHooks = []StaffHook{}

	AddStaffHook(boil.AfterSelectHook, staffAfterSelectHook)
	if err = o.doAfterSelectHooks(nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	staffAfterSelectHooks = []StaffHook{}

	AddStaffHook(boil.BeforeUpdateHook, staffBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	staffBeforeUpdateHooks = []StaffHook{}

	AddStaffHook(boil.AfterUpdateHook, staffAfterUpdateHook)
	if err = o.doAfterUpdateHooks(nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	staffAfterUpdateHooks = []StaffHook{}

	AddStaffHook(boil.BeforeDeleteHook, staffBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	staffBeforeDeleteHooks = []StaffHook{}

	AddStaffHook(boil.AfterDeleteHook, staffAfterDeleteHook)
	if err = o.doAfterDeleteHooks(nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	staffAfterDeleteHooks = []StaffHook{}

	AddStaffHook(boil.BeforeUpsertHook, staffBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	staffBeforeUpsertHooks = []StaffHook{}

	AddStaffHook(boil.AfterUpsertHook, staffAfterUpsertHook)
	if err = o.doAfterUpsertHooks(nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	staffAfterUpsertHooks = []StaffHook{}
}
func testStaffsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	staff := &Staff{}
	if err = randomize.Struct(seed, staff, staffDBTypes, true, staffColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = staff.Insert(tx); err != nil {
		t.Error(err)
	}

	count, err := Staffs(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testStaffsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	staff := &Staff{}
	if err = randomize.Struct(seed, staff, staffDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = staff.Insert(tx, staffColumnsWithoutDefault...); err != nil {
		t.Error(err)
	}

	count, err := Staffs(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testStaffOneToOneStoreUsingManagerStaffStore(t *testing.T) {
	tx := MustTx(boil.Begin())
	defer tx.Rollback()

	var foreign Store
	var local Staff

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &foreign, storeDBTypes, true, storeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Store struct: %s", err)
	}
	if err := randomize.Struct(seed, &local, staffDBTypes, true, staffColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}

	if err := local.Insert(tx); err != nil {
		t.Fatal(err)
	}

	foreign.ManagerStaffID = local.StaffID
	if err := foreign.Insert(tx); err != nil {
		t.Fatal(err)
	}

	check, err := local.ManagerStaffStore(tx).One()
	if err != nil {
		t.Fatal(err)
	}

	if check.ManagerStaffID != foreign.ManagerStaffID {
		t.Errorf("want: %v, got %v", foreign.ManagerStaffID, check.ManagerStaffID)
	}

	slice := StaffSlice{&local}
	if err = local.L.LoadManagerStaffStore(tx, false, (*[]*Staff)(&slice)); err != nil {
		t.Fatal(err)
	}
	if local.R.ManagerStaffStore == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.ManagerStaffStore = nil
	if err = local.L.LoadManagerStaffStore(tx, true, &local); err != nil {
		t.Fatal(err)
	}
	if local.R.ManagerStaffStore == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testStaffOneToOneSetOpStoreUsingManagerStaffStore(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer tx.Rollback()

	var a Staff
	var b, c Store

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, staffDBTypes, false, strmangle.SetComplement(staffPrimaryKeyColumns, staffColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, storeDBTypes, false, strmangle.SetComplement(storePrimaryKeyColumns, storeColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, storeDBTypes, false, strmangle.SetComplement(storePrimaryKeyColumns, storeColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(tx); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Store{&b, &c} {
		err = a.SetManagerStaffStore(tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.ManagerStaffStore != x {
			t.Error("relationship struct not set to correct value")
		}
		if x.R.ManagerStaff != &a {
			t.Error("failed to append to foreign relationship struct")
		}

		if a.StaffID != x.ManagerStaffID {
			t.Error("foreign key was wrong value", a.StaffID)
		}

		zero := reflect.Zero(reflect.TypeOf(x.ManagerStaffID))
		reflect.Indirect(reflect.ValueOf(&x.ManagerStaffID)).Set(zero)

		if err = x.Reload(tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.StaffID != x.ManagerStaffID {
			t.Error("foreign key was wrong value", a.StaffID, x.ManagerStaffID)
		}

		if err = x.Delete(tx); err != nil {
			t.Fatal("failed to delete x", err)
		}
	}
}
func testStaffToManyPayments(t *testing.T) {
	var err error
	tx := MustTx(boil.Begin())
	defer tx.Rollback()

	var a Staff
	var b, c Payment

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, staffDBTypes, true, staffColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}

	if err := a.Insert(tx); err != nil {
		t.Fatal(err)
	}

	randomize.Struct(seed, &b, paymentDBTypes, false, paymentColumnsWithDefault...)
	randomize.Struct(seed, &c, paymentDBTypes, false, paymentColumnsWithDefault...)

	b.StaffID = a.StaffID
	c.StaffID = a.StaffID
	if err = b.Insert(tx); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx); err != nil {
		t.Fatal(err)
	}

	payment, err := a.Payments(tx).All()
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range payment {
		if v.StaffID == b.StaffID {
			bFound = true
		}
		if v.StaffID == c.StaffID {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := StaffSlice{&a}
	if err = a.L.LoadPayments(tx, false, (*[]*Staff)(&slice)); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.Payments); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.Payments = nil
	if err = a.L.LoadPayments(tx, true, &a); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.Payments); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", payment)
	}
}

func testStaffToManyRentals(t *testing.T) {
	var err error
	tx := MustTx(boil.Begin())
	defer tx.Rollback()

	var a Staff
	var b, c Rental

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, staffDBTypes, true, staffColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}

	if err := a.Insert(tx); err != nil {
		t.Fatal(err)
	}

	randomize.Struct(seed, &b, rentalDBTypes, false, rentalColumnsWithDefault...)
	randomize.Struct(seed, &c, rentalDBTypes, false, rentalColumnsWithDefault...)

	b.StaffID = a.StaffID
	c.StaffID = a.StaffID
	if err = b.Insert(tx); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx); err != nil {
		t.Fatal(err)
	}

	rental, err := a.Rentals(tx).All()
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range rental {
		if v.StaffID == b.StaffID {
			bFound = true
		}
		if v.StaffID == c.StaffID {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := StaffSlice{&a}
	if err = a.L.LoadRentals(tx, false, (*[]*Staff)(&slice)); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.Rentals); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.Rentals = nil
	if err = a.L.LoadRentals(tx, true, &a); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.Rentals); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", rental)
	}
}

func testStaffToManyAddOpPayments(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer tx.Rollback()

	var a Staff
	var b, c, d, e Payment

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, staffDBTypes, false, strmangle.SetComplement(staffPrimaryKeyColumns, staffColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*Payment{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, paymentDBTypes, false, strmangle.SetComplement(paymentPrimaryKeyColumns, paymentColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(tx); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*Payment{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddPayments(tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if a.StaffID != first.StaffID {
			t.Error("foreign key was wrong value", a.StaffID, first.StaffID)
		}
		if a.StaffID != second.StaffID {
			t.Error("foreign key was wrong value", a.StaffID, second.StaffID)
		}

		if first.R.Staff != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.Staff != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.Payments[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.Payments[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.Payments(tx).Count()
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}
func testStaffToManyAddOpRentals(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer tx.Rollback()

	var a Staff
	var b, c, d, e Rental

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, staffDBTypes, false, strmangle.SetComplement(staffPrimaryKeyColumns, staffColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*Rental{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, rentalDBTypes, false, strmangle.SetComplement(rentalPrimaryKeyColumns, rentalColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(tx); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*Rental{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddRentals(tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if a.StaffID != first.StaffID {
			t.Error("foreign key was wrong value", a.StaffID, first.StaffID)
		}
		if a.StaffID != second.StaffID {
			t.Error("foreign key was wrong value", a.StaffID, second.StaffID)
		}

		if first.R.Staff != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.Staff != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.Rentals[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.Rentals[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.Rentals(tx).Count()
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}
func testStaffToOneAddressUsingAddress(t *testing.T) {
	tx := MustTx(boil.Begin())
	defer tx.Rollback()

	var local Staff
	var foreign Address

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, staffDBTypes, false, staffColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, addressDBTypes, false, addressColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Address struct: %s", err)
	}

	if err := foreign.Insert(tx); err != nil {
		t.Fatal(err)
	}

	local.AddressID = foreign.AddressID
	if err := local.Insert(tx); err != nil {
		t.Fatal(err)
	}

	check, err := local.Address(tx).One()
	if err != nil {
		t.Fatal(err)
	}

	if check.AddressID != foreign.AddressID {
		t.Errorf("want: %v, got %v", foreign.AddressID, check.AddressID)
	}

	slice := StaffSlice{&local}
	if err = local.L.LoadAddress(tx, false, (*[]*Staff)(&slice)); err != nil {
		t.Fatal(err)
	}
	if local.R.Address == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Address = nil
	if err = local.L.LoadAddress(tx, true, &local); err != nil {
		t.Fatal(err)
	}
	if local.R.Address == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testStaffToOneStoreUsingStore(t *testing.T) {
	tx := MustTx(boil.Begin())
	defer tx.Rollback()

	var local Staff
	var foreign Store

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, staffDBTypes, false, staffColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, storeDBTypes, false, storeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Store struct: %s", err)
	}

	if err := foreign.Insert(tx); err != nil {
		t.Fatal(err)
	}

	local.StoreID = foreign.StoreID
	if err := local.Insert(tx); err != nil {
		t.Fatal(err)
	}

	check, err := local.Store(tx).One()
	if err != nil {
		t.Fatal(err)
	}

	if check.StoreID != foreign.StoreID {
		t.Errorf("want: %v, got %v", foreign.StoreID, check.StoreID)
	}

	slice := StaffSlice{&local}
	if err = local.L.LoadStore(tx, false, (*[]*Staff)(&slice)); err != nil {
		t.Fatal(err)
	}
	if local.R.Store == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Store = nil
	if err = local.L.LoadStore(tx, true, &local); err != nil {
		t.Fatal(err)
	}
	if local.R.Store == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testStaffToOneSetOpAddressUsingAddress(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer tx.Rollback()

	var a Staff
	var b, c Address

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, staffDBTypes, false, strmangle.SetComplement(staffPrimaryKeyColumns, staffColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, addressDBTypes, false, strmangle.SetComplement(addressPrimaryKeyColumns, addressColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, addressDBTypes, false, strmangle.SetComplement(addressPrimaryKeyColumns, addressColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(tx); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Address{&b, &c} {
		err = a.SetAddress(tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Address != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.Staffs[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.AddressID != x.AddressID {
			t.Error("foreign key was wrong value", a.AddressID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.AddressID))
		reflect.Indirect(reflect.ValueOf(&a.AddressID)).Set(zero)

		if err = a.Reload(tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.AddressID != x.AddressID {
			t.Error("foreign key was wrong value", a.AddressID, x.AddressID)
		}
	}
}
func testStaffToOneSetOpStoreUsingStore(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer tx.Rollback()

	var a Staff
	var b, c Store

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, staffDBTypes, false, strmangle.SetComplement(staffPrimaryKeyColumns, staffColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, storeDBTypes, false, strmangle.SetComplement(storePrimaryKeyColumns, storeColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, storeDBTypes, false, strmangle.SetComplement(storePrimaryKeyColumns, storeColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(tx); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Store{&b, &c} {
		err = a.SetStore(tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Store != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.Staffs[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.StoreID != x.StoreID {
			t.Error("foreign key was wrong value", a.StoreID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.StoreID))
		reflect.Indirect(reflect.ValueOf(&a.StoreID)).Set(zero)

		if err = a.Reload(tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.StoreID != x.StoreID {
			t.Error("foreign key was wrong value", a.StoreID, x.StoreID)
		}
	}
}
func testStaffsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	staff := &Staff{}
	if err = randomize.Struct(seed, staff, staffDBTypes, true, staffColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = staff.Insert(tx); err != nil {
		t.Error(err)
	}

	if err = staff.Reload(tx); err != nil {
		t.Error(err)
	}
}

func testStaffsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	staff := &Staff{}
	if err = randomize.Struct(seed, staff, staffDBTypes, true, staffColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = staff.Insert(tx); err != nil {
		t.Error(err)
	}

	slice := StaffSlice{staff}

	if err = slice.ReloadAll(tx); err != nil {
		t.Error(err)
	}
}
func testStaffsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	staff := &Staff{}
	if err = randomize.Struct(seed, staff, staffDBTypes, true, staffColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = staff.Insert(tx); err != nil {
		t.Error(err)
	}

	slice, err := Staffs(tx).All()
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	staffDBTypes = map[string]string{`Active`: `tinyint`, `AddressID`: `smallint`, `Email`: `varchar`, `FirstName`: `varchar`, `LastName`: `varchar`, `LastUpdate`: `timestamp`, `Password`: `varchar`, `Picture`: `blob`, `StaffID`: `tinyint`, `StoreID`: `tinyint`, `Username`: `varchar`}
	_            = bytes.MinRead
)

func testStaffsUpdate(t *testing.T) {
	t.Parallel()

	if len(staffColumns) == len(staffPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	staff := &Staff{}
	if err = randomize.Struct(seed, staff, staffDBTypes, true, staffColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = staff.Insert(tx); err != nil {
		t.Error(err)
	}

	count, err := Staffs(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, staff, staffDBTypes, true, staffColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}

	if err = staff.Update(tx); err != nil {
		t.Error(err)
	}
}

func testStaffsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(staffColumns) == len(staffPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	staff := &Staff{}
	if err = randomize.Struct(seed, staff, staffDBTypes, true, staffColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = staff.Insert(tx); err != nil {
		t.Error(err)
	}

	count, err := Staffs(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, staff, staffDBTypes, true, staffPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(staffColumns, staffPrimaryKeyColumns) {
		fields = staffColumns
	} else {
		fields = strmangle.SetComplement(
			staffColumns,
			staffPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(staff))
	updateMap := M{}
	for _, col := range fields {
		updateMap[col] = value.FieldByName(strmangle.TitleCase(col)).Interface()
	}

	slice := StaffSlice{staff}
	if err = slice.UpdateAll(tx, updateMap); err != nil {
		t.Error(err)
	}
}
func testStaffsUpsert(t *testing.T) {
	t.Parallel()

	if len(staffColumns) == len(staffPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	staff := Staff{}
	if err = randomize.Struct(seed, &staff, staffDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = staff.Upsert(tx, nil); err != nil {
		t.Errorf("Unable to upsert Staff: %s", err)
	}

	count, err := Staffs(tx).Count()
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &staff, staffDBTypes, false, staffPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Staff struct: %s", err)
	}

	if err = staff.Upsert(tx, nil); err != nil {
		t.Errorf("Unable to upsert Staff: %s", err)
	}

	count, err = Staffs(tx).Count()
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
